#include <bits/stdc++.h>
#include <unistd.h>
using namespace std;
int a[100000] = {0};
int main(){
    int n, x, y;
    scanf("%d", &n);
    sleep(10);
    while(n--){
        scanf("%d%d", &x, &y);
        printf("%d\n", x - y);
    }
    return 0;
}
